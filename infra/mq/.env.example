# Copia este archivo a ".env" en la misma carpeta y cambia el valor por una
# clave propia (cualquier string secreto sirve). Debe ser IGUAL para todos
# los nodos del cluster: es lo que les permite autenticarse entre si.
RABBITMQ_ERLANG_COOKIE=cambia-esta-clave-por-una-propia
